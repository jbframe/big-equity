# Loaded by `npm run dev` (Vite development mode); production builds ignore
# this file. Targets the local stack's API alias — see .env.example for other
# setups, and .env.local (gitignored) for personal overrides.
VITE_API_BASE_URL=http://local.api.makejohnacoffee.com
